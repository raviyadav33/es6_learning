window .onload=function(){
    /*Const means we assing a value we cannot change the new value /set the new value */
    const pi=3.14; 
    // console .log(pi);
    /*lets try to calculate the area of circle*/
    function calArea(r){
        console.log('Area of the circle :'+pi*r*r);
    } 
    calArea();
}